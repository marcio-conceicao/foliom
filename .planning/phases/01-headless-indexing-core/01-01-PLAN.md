---
phase: 01-headless-indexing-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - .gitattributes
  - .github/workflows/ci.yml
  - crates/core/Cargo.toml
  - crates/core/src/lib.rs
  - crates/core/src/parser/mod.rs
  - crates/core/src/parser/segment.rs
  - crates/core/tests/roundtrip.rs
  - crates/cli/Cargo.toml
  - crates/cli/src/main.rs
autonomous: true
requirements: [PRS-07, ACPT-01, ACPT-04]

must_haves:
  truths:
    - "Cargo workspace builds clean on Linux/macOS/Windows with `cargo build --workspace --locked`."
    - "Round-trip test exists at crates/core/tests/roundtrip.rs and runs against every .md file in data-folder-sample/Logseq/."
    - "The test FAILS at the end of this plan (segmenter is a deliberate stub) — proving the gate is wired before any implementation."
    - "GitHub Actions matrix runs the round-trip test on ubuntu-latest, macos-latest, windows-latest."
    - "`.gitattributes` forces LF on all `.md` files so the corpus is byte-stable across OSes."
  artifacts:
    - path: "Cargo.toml"
      provides: "Workspace manifest declaring crates/core and crates/cli"
      contains: "[workspace]"
    - path: "crates/core/src/parser/segment.rs"
      provides: "RawBlock + RawDrawer types and stub segment() returning vec![]"
      exports: ["RawBlock", "RawDrawer", "segment"]
    - path: "crates/core/tests/roundtrip.rs"
      provides: "ACPT-01 round-trip property test iterating the corpus"
      contains: "roundtrip_byte_identical_for_entire_corpus"
    - path: ".github/workflows/ci.yml"
      provides: "Cross-platform CI matrix (ACPT-04)"
      contains: "matrix:"
    - path: ".gitattributes"
      provides: "LF-only enforcement for round-trip stability across OSes"
      contains: "eol=lf"
  key_links:
    - from: "crates/core/tests/roundtrip.rs"
      to: "crates/core/src/parser/segment.rs::segment"
      via: "use foliom_core::parser::segment::segment"
      pattern: "use foliom_core::parser"
    - from: ".github/workflows/ci.yml"
      to: "crates/core/tests/roundtrip.rs"
      via: "cargo nextest run --workspace"
      pattern: "cargo nextest run"
---

<objective>
Ship the Cargo workspace skeleton and the **failing** round-trip CI gate before any parser/storage/indexer code is written. This plan establishes the non-negotiable ordering invariant from ROADMAP §"Phase 1 ordering invariant": ACPT-01 ships first, and every later plan must keep it green.

Purpose: Lock in the byte-identical round-trip contract as the foundational property test. The very first commit of the parser (Plan 02) flips this test from red to green; nothing else in the phase can land without it passing.
Output: Cargo workspace at the repo root, two crates (`crates/core`, `crates/cli`), a `RawBlock` type, a stub `segment()` returning empty Vec, a round-trip test iterating all 619 sample files (expected RED), and a GitHub Actions matrix wired for all three OSes plus a `.gitattributes` enforcing LF.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-headless-indexing-core/01-CONTEXT.md
@.planning/phases/01-headless-indexing-core/01-RESEARCH.md
@.planning/research/PITFALLS.md

<interfaces>
<!-- This plan CREATES interfaces consumed by Plan 02 and beyond. -->

Types to define in crates/core/src/parser/segment.rs (per RESEARCH §Two-Stage Parser → Stage 1):

```rust
#[derive(Debug, Clone)]
pub struct RawBlock {
    pub depth: u8,            // TAB count; u8::MAX = page prelude sentinel
    pub byte_offset: usize,   // absolute byte offset into source
    pub byte_length: usize,   // inclusive of continuation, drawers, properties, trailing newline
    pub raw: String,
    pub properties: Vec<(String, String)>,
    pub drawers: Vec<RawDrawer>,
}

#[derive(Debug, Clone)]
pub struct RawDrawer {
    pub name: String,
    pub byte_offset: usize,
    pub byte_length: usize,
}

pub fn segment(source: &[u8]) -> Vec<RawBlock>;
```

Stub body for this plan: `pub fn segment(_source: &[u8]) -> Vec<RawBlock> { Vec::new() }`. Plan 02 replaces the body with the real state machine.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Cargo workspace, two crates, and RawBlock types with stub segment()</name>
  <files>Cargo.toml, crates/core/Cargo.toml, crates/core/src/lib.rs, crates/core/src/parser/mod.rs, crates/core/src/parser/segment.rs, crates/cli/Cargo.toml, crates/cli/src/main.rs</files>
  <action>Create a Cargo workspace per D-07. Root `Cargo.toml` declares `[workspace]` with `members = ["crates/core", "crates/cli"]` and `resolver = "2"`. Set workspace-wide `[workspace.package]` with `rust-version = "1.85"`, `edition = "2024"` (fall back to `"2021"` if 1.85 toolchain doesn't yet expose 2024 edition — document the fallback in a comment and proceed; D-09 says edition 2024 "when available").

`crates/core/Cargo.toml`: package name `foliom-core`, library crate, no dependencies yet (Plan 02 adds the segmenter; Plan 03 adds `pulldown-cmark`/`unicode-normalization`; Plan 04 adds `rusqlite`/`blake3`/`rusqlite_migration`). Add `[dev-dependencies] walkdir = "2.5"` only — needed by the round-trip test in Task 3.

`crates/core/src/lib.rs`: declare `pub mod parser;` and nothing else. `crates/core/src/parser/mod.rs`: declare `pub mod segment;`. `crates/core/src/parser/segment.rs`: define `RawBlock`, `RawDrawer`, and `pub fn segment(_source: &[u8]) -> Vec&lt;RawBlock&gt; { Vec::new() }` with doc comments referencing PRS-01..PRS-03 and the invariants from RESEARCH §Stage 1 (contiguous non-overlapping byte ranges; concat == source). The stub is intentional — Plan 02 implements the state machine.

`crates/cli/Cargo.toml`: package name `foliom-cli`, binary crate, name `foliom`. No dependencies yet (Plan 07 adds `clap`/`serde`/`anyhow`). `crates/cli/src/main.rs`: `fn main() { eprintln!("foliom: not yet implemented"); std::process::exit(2); }`.

Do NOT add `serialize` / `to_markdown` / `format_block` anywhere — per AP-2 in RESEARCH §Anti-Patterns, no serialize function exists in Phase 1. The only way to write a file is byte-splice (Phase 3).</action>
  <verify>
    <automated>cd /home/mconceicao/work-others/foliom &amp;&amp; cargo build --workspace --locked 2>&amp;1 | tail -20 &amp;&amp; cargo metadata --no-deps --format-version 1 | grep -c '"name":"foliom-core"\|"name":"foliom-cli"'</automated>
  </verify>
  <done>Workspace builds clean. Both `foliom-core` (lib) and `foliom-cli` (bin `foliom`) appear in `cargo metadata`. `segment()` compiles and returns `vec![]`. No serialize/to_markdown helpers exist anywhere (`grep -r "fn serialize\|fn to_markdown\|fn format_block" crates/` returns nothing).</done>
</task>

<task type="auto">
  <name>Task 2: Enforce LF line endings via .gitattributes and add corpus-checksum CI assertion script</name>
  <files>.gitattributes</files>
  <action>Create `.gitattributes` at repo root with the exact contents from RESEARCH §Cross-Platform CI Matrix → Line-ending discipline:

```
* text=auto eol=lf
data-folder-sample/** -text
*.md text eol=lf
```

This is load-bearing for ACPT-01: Windows checkouts must not CRLF-mangle the corpus, or the round-trip gate fails for the wrong reason (CRLF drift, not parser drift). Per RESEARCH §Anti-Patterns AP-3 + cross-platform gotchas table.

Do NOT run `git add --renormalize` here — that mutates the working tree and is outside the plan's scope. The `.gitattributes` file itself is the deliverable; renormalization happens organically when the repo is re-checked-out on CI runners.

Note for the verification step: locally we cannot easily assert that Windows checkout will be LF without booting a Windows runner. We rely on the CI matrix (Task 4) to catch any leak. As a local guard, the round-trip test in Task 3 will fail loudly with a visible-TAB/CR diff if CRLF ever sneaks in.</action>
  <verify>
    <automated>cd /home/mconceicao/work-others/foliom &amp;&amp; test -f .gitattributes &amp;&amp; grep -q 'eol=lf' .gitattributes &amp;&amp; grep -q 'data-folder-sample/\*\* -text' .gitattributes &amp;&amp; echo OK</automated>
  </verify>
  <done>`.gitattributes` exists and contains the three required lines (`* text=auto eol=lf`, `data-folder-sample/** -text`, `*.md text eol=lf`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the failing round-trip property test against the full Logseq sample corpus (ACPT-01)</name>
  <files>crates/core/tests/roundtrip.rs</files>
  <behavior>
    - Test enumerates every `*.md` file under `data-folder-sample/Logseq/` (resolved via `env!("CARGO_MANIFEST_DIR")` joined with `"../../data-folder-sample/Logseq"` — AP-4).
    - Skips any path containing a `logseq` component (the config dir is not `.md` content for round-trip purposes, per the IDX-01 ignore list).
    - For each file: reads bytes, calls `segment(&bytes)`, reconstructs by concatenating `bytes[b.byte_offset..b.byte_offset+b.byte_length]` for each block in order, asserts byte equality.
    - When the corpus has N>0 files but `segment()` returns `vec![]`, the reconstruction is empty and the test MUST fail with a clear "want N bytes, got 0 bytes" diff on the first file. This is the expected RED state at the end of this plan.
    - Asserts the corpus size is exactly 619 files (assumption A1 from RESEARCH); if the corpus drifts, the assertion message tells the next planner to update the count.
    - Bounds output: collects at most 3 failing files into a `Vec` and reports them; does not flood stdout with all 619 diffs.
    - Helper `first_diff_report(want, got)` shows the first byte where the buffers diverge, with TAB → `\t` and CR → `\r` substitution so the diff is human-readable.
  </behavior>
  <action>Copy the test design from RESEARCH §Round-Trip CI Gate verbatim (it is a complete file). Adjust only the import path: `use foliom_core::parser::segment::segment;`. The helper functions `first_diff_report` and `visible` are included as in RESEARCH.

Mark this test with `#[test]` (not `#[ignore]`). It MUST run by default in `cargo nextest run --workspace`. At this point it WILL FAIL — that is the entire point of Plan 1: the gate exists and is RED before any parser implementation lands. Plan 02 turns it GREEN.

Add a comment at the top of the file: `// ACPT-01 — round-trip byte-identical gate. Ships RED in plan 01-01; flipped GREEN in plan 01-02. MUST stay green for the life of the project.`

Confirm `walkdir = "2.5"` is in `[dev-dependencies]` of `crates/core/Cargo.toml` (added in Task 1). No other deps needed — the test uses only `std::fs`, `std::path`, and `walkdir`.</action>
  <verify>
    <automated>cd /home/mconceicao/work-others/foliom &amp;&amp; cargo test --test roundtrip --package foliom-core 2>&amp;1 | tail -30 | grep -E "FAILED|test result: FAILED|round-trip drift|got 0 bytes|want [0-9]+ bytes"</automated>
  </verify>
  <done>The test compiles, runs, and FAILS as expected because `segment()` is a stub. The failure output names the first file and shows a "want N / got 0" style diff. Running `cargo test --test roundtrip` exits non-zero. This is the desired RED state — Plan 02 will turn it GREEN.</done>
</task>

<task type="auto">
  <name>Task 4: Wire GitHub Actions cross-platform CI matrix (ACPT-04)</name>
  <files>.github/workflows/ci.yml</files>
  <action>Create `.github/workflows/ci.yml` from the template in RESEARCH §Cross-Platform CI Matrix. Required elements:

1. `on: [push, pull_request]`.
2. Matrix: `os: [ubuntu-latest, macos-latest, windows-latest]`, `rust: [stable]`, `fail-fast: false`.
3. Steps: `actions/checkout@v4`, `dtolnay/rust-toolchain@stable`, `Swatinem/rust-cache@v2` keyed per-OS, `taiki-e/install-action@nextest`.
4. Build step: `cargo build --workspace --locked`.
5. Test step: `cargo nextest run --workspace --no-fail-fast`.
6. Inventory smoke steps from RESEARCH — guarded with `if: runner.os != 'Windows'` and a Windows-specific twin using `$env:TEMP`. Mark both with `continue-on-error: true` for now (Plan 07 implements `foliom inventory`; until then the step exits non-zero, which we tolerate at this stage). Add a TODO comment: `# TODO(plan 01-07): remove continue-on-error once inventory subcommand lands.`

At the end of Plan 01, CI status is **expected RED on the test step** (round-trip fails — segmenter is a stub). This is correct: the matrix exists, runs, and reports failure on a known cause. Plan 02 makes it green.

Do NOT add `--all-features` or feature flag matrices. Phase 1 has no features yet. Do NOT add release-build steps; CI is debug-only for the foundation phase.</action>
  <verify>
    <automated>cd /home/mconceicao/work-others/foliom &amp;&amp; test -f .github/workflows/ci.yml &amp;&amp; grep -q 'ubuntu-latest' .github/workflows/ci.yml &amp;&amp; grep -q 'macos-latest' .github/workflows/ci.yml &amp;&amp; grep -q 'windows-latest' .github/workflows/ci.yml &amp;&amp; grep -q 'cargo nextest run' .github/workflows/ci.yml &amp;&amp; echo OK</automated>
  </verify>
  <done>`.github/workflows/ci.yml` exists with the three-OS matrix, installs `cargo-nextest`, runs `cargo build --workspace --locked` and `cargo nextest run --workspace`. Inventory step is wired with `continue-on-error: true` and a TODO referencing Plan 07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → test harness | Round-trip test reads `data-folder-sample/Logseq/` — content is part of the repo, fully trusted. |
| GitHub Actions runner → repo checkout | Untrusted only insofar as `autocrlf` could mangle bytes; addressed by `.gitattributes`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | Windows Git checkout (autocrlf) | mitigate | `.gitattributes` forces `eol=lf` for `*.md` and `-text` for `data-folder-sample/**`; round-trip diff helper makes CRLF leaks visible as `\r` in output. |
| T-01-02 | Tampering | CI cache corruption | accept | Cache key includes OS; rebuild from scratch on key miss. Cache only speeds up builds; never affects test correctness. |
| T-01-03 | Denial of service | Stub `segment()` consumes 619 files in O(1) returning empty Vec | accept | Test bounds output to 3 failures and uses `break;` after collecting enough — no unbounded memory growth even on full RED. |
| T-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan. `walkdir` is locked to 2.5 (PRD-locked + RESEARCH §Don't Hand-Roll). All cargo deps are listed in CLAUDE.md "Technology Stack" with HIGH confidence. No `[ASSUMED]`/`[SUS]` packages — legitimacy gate not triggered. |
</threat_model>

<verification>
- `cargo build --workspace --locked` succeeds on local dev machine.
- `cargo test --test roundtrip --package foliom-core` **fails** (expected RED — stub segmenter).
- `cargo metadata --no-deps` shows both crates with the right names.
- No `serialize` / `to_markdown` / `format_block` functions exist anywhere in the workspace (AP-2 guard).
- `.gitattributes` and `.github/workflows/ci.yml` exist with the required content.
</verification>

<success_criteria>
1. Workspace builds clean (`cargo build --workspace --locked` exits 0).
2. Round-trip test exists, runs, and fails because `segment()` returns `vec![]`. The failure message names a specific file and shows a "want N / got 0" diff.
3. CI matrix configuration covers Linux + macOS + Windows.
4. `.gitattributes` enforces LF on `.md` files.
5. AP-2 guard: no serialize helper exists in Phase 1 code.
</success_criteria>

<output>
Create `.planning/phases/01-headless-indexing-core/01-01-SUMMARY.md` when done — describe the RED state of the gate, the workspace layout, and confirm the file count assumption (A1) against the actual corpus.
</output>
