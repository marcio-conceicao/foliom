---
phase: 04-disk-sync
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - frontend/src/lib/stores/watcher.ts
  - frontend/src/lib/watcher.ts
  - frontend/src/App.svelte
  - frontend/src/lib/pages/PageView.svelte
  - frontend/src/lib/components/Sidebar.svelte
  - frontend/src/__tests__/watcher.test.ts
autonomous: true
requirements: [SNC-06]
tags: [phase-4, frontend, sse, eventsource, svelte, conflict-banner, watcher-status]

must_haves:
  truths:
    - "A single global EventSource per tab subscribes to /api/watch/events; no per-component instances"
    - "pages_updated event for the currently viewed page silently reloads if no block is in edit"
    - "pages_updated event while a block is in edit sets staleConflict = true, showing the Phase 3 banner"
    - "index_reset event triggers fetchPage(currentPageName) unconditionally"
    - "Sidebar pill shows green/spinning/grey dot based on watcherStatus store ('connected'/'reconnecting'/'offline')"
    - "After 10s without reconnect on error, watcherStatus transitions to 'offline'"
    - "Navigating between pages does not create a second EventSource"
  artifacts:
    - path: "frontend/src/lib/stores/watcher.ts"
      provides: "watcherStatus store ('connected'|'reconnecting'|'offline') + externalConflict store"
    - path: "frontend/src/lib/watcher.ts"
      provides: "startWatcher() / stopWatcher() singleton with event handlers"
    - path: "frontend/src/__tests__/watcher.test.ts"
      provides: "Vitest tests for store transitions + PageView conflict path (SNC-06)"
  key_links:
    - from: "frontend/src/lib/watcher.ts"
      to: "frontend/src/lib/stores/watcher.ts"
      via: "watcherStatus.set('connected' | 'reconnecting')"
      pattern: "watcherStatus.set"
    - from: "frontend/src/lib/watcher.ts"
      to: "frontend/src/lib/pages/PageView.svelte"
      via: "externalConflict store → staleConflict = true"
      pattern: "externalConflict"
    - from: "frontend/src/App.svelte"
      to: "frontend/src/lib/watcher.ts"
      via: "onMount(() => startWatcher())"
      pattern: "startWatcher"
---

<objective>
Add the Svelte 5 frontend layer for Phase 4: a singleton EventSource that subscribes to the backend SSE endpoint, drives live-reload and conflict detection, and surfaces a watcher-status pill in the Sidebar. Reuses Phase 3's existing `staleConflict` state and `reload()` function in PageView.svelte for the conflict banner — no new banner component needed.

Purpose: Fulfills SNC-06 (conflict UI for concurrent edit + external change) and the frontend-observable half of SNC-03 (live-reload on external edits).
Output: watcher.ts, stores/watcher.ts, App.svelte update, PageView.svelte update, Sidebar.svelte update, Vitest tests.
</objective>

<execution_context>
@/home/mconceicao/work-others/foliom/.claude/get-shit-done/workflows/execute-plan.md
@/home/mconceicao/work-others/foliom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/mconceicao/work-others/foliom/.planning/PROJECT.md
@/home/mconceicao/work-others/foliom/.planning/ROADMAP.md
@/home/mconceicao/work-others/foliom/.planning/phases/04-disk-sync/04-CONTEXT.md
@/home/mconceicao/work-others/foliom/.planning/phases/04-disk-sync/04-RESEARCH.md
@/home/mconceicao/work-others/foliom/.planning/phases/04-disk-sync/04-01-SUMMARY.md

<interfaces>
<!-- Key stores and patterns the executor needs. Read PageView.svelte + stores before writing. -->

From frontend/src/lib/pages/PageView.svelte (Phase 3 shipped):
  // staleConflict is a local $state variable (Svelte 5 rune)
  let staleConflict = $state(false);
  // reload() function reloads the current page and clears the editor
  function reload(): void { /* fetches page + clears currentlyEditing */ }
  // The existing banner is rendered conditionally on staleConflict
  // Phase 4 watcher path must SET staleConflict = true from outside the component

From frontend/src/lib/stores/editing.ts (Phase 3 shipped):
  // currentlyEditing is a writable Svelte store holding the block id being edited, or null
  export const currentlyEditing: Writable<number | null>;

From frontend/src/lib/stores/ (Phase 2 shipped, check existing files):
  // currentPage holds the currently displayed page object
  export const currentPage: Writable<PageDetail | null>;

New stores to create in frontend/src/lib/stores/watcher.ts:
  export const watcherStatus: Writable<'connected' | 'reconnecting' | 'offline'>;
  // externalConflict: set by watcher.ts when pages_updated arrives for editing page
  export const externalConflict: Writable<{ newFileHash: string } | null>;

New composable in frontend/src/lib/watcher.ts:
  export function startWatcher(): void;
  export function stopWatcher(): void;
  // Module-level singleton: let es: EventSource | null = null;

PageView.svelte must subscribe to externalConflict store:
  $effect(() => {
    const conflict = $externalConflict;
    if (conflict !== null && $currentlyEditing !== null) {
      staleConflict = true;        // triggers existing banner
      externalConflict.set(null);  // consume
    } else if (conflict !== null && $currentlyEditing === null) {
      // silent reload — no editor active
      reload();
      externalConflict.set(null);
    }
  });
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: watcher stores + singleton EventSource composable + Vitest tests</name>
  <files>
    frontend/src/lib/stores/watcher.ts,
    frontend/src/lib/watcher.ts,
    frontend/src/__tests__/watcher.test.ts
  </files>
  <behavior>
    - test watcherStatus_transitions_on_open: create mock EventSource, fire 'open' event, assert watcherStatus.get() === 'connected'.
    - test watcherStatus_transitions_on_error: fire 'error' event, assert watcherStatus.get() === 'reconnecting'; wait 10001ms (fake timers), assert 'offline'.
    - test pages_updated_sets_externalConflict: currentlyEditing = 42, currentPage = {name:'foo'}, fire MessageEvent('pages_updated', {data: JSON.stringify([{name:'foo', file_hash:'abc'}])}), assert externalConflict.get() = {newFileHash:'abc'}.
    - test pages_updated_no_conflict_when_not_editing: currentlyEditing = null, fire pages_updated for current page, assert externalConflict.get() === null (fetchPage called instead — mock and assert).
    - test pages_updated_different_page_ignored: currentPage = {name:'bar'}, fire pages_updated for {name:'foo'}, assert externalConflict.get() === null AND fetchPage not called.
    - test index_reset_calls_fetchPage: fire MessageEvent('index_reset', {data:'{}'}), assert fetchPage called with currentPage.name.
    - test no_duplicate_EventSource_on_double_startWatcher: call startWatcher() twice, assert only one EventSource created (close count = 0 on second call).
  </behavior>
  <action>
    frontend/src/lib/stores/watcher.ts (NEW):
    Create and export watcherStatus writable store (initial 'reconnecting' — becomes 'connected' on first open event). Create and export externalConflict store (initial null). Both use Svelte 5 writable from svelte/store.

    frontend/src/lib/watcher.ts (NEW):
    Implement module-level singleton pattern (per PITFALL 3 in RESEARCH — never per-component):
    - let es: EventSource | null = null
    - let offlineTimer: ReturnType<typeof setTimeout> | null = null
    - export function startWatcher(): void — if es already exists and es.readyState !== EventSource.CLOSED, return immediately (no duplicate). Create new EventSource('/api/watch/events').
    - Register 'open' handler: watcherStatus.set('connected'); clear offlineTimer.
    - Register 'error' handler: watcherStatus.set('reconnecting'); set offlineTimer to setTimeout(() => watcherStatus.set('offline'), 10_000).
    - Register 'pages_updated' handler: parse e.data as {name: string, fileHash: string}[] (camelCase — match backend serde naming). Get currentPage from store. If updated page name matches currentPage.name: if get(currentlyEditing) !== null → externalConflict.set({newFileHash: matchedPage.fileHash}) ; else → fetchPage(currentPage.name).then(fresh => currentPage.set(fresh)).
    - Register 'index_reset' handler: get currentPage from store; if set → fetchPage(currentPage.name).then(fresh => currentPage.set(fresh)).
    - export function stopWatcher(): void — es?.close(); es = null; clearOfflineTimer(). Called on page unload.

    Note on backend JSON field naming: backend emits snake_case in serde (file_hash), but the research pattern shows camelCase. Read the actual backend DTO to confirm; if snake_case, parse as file_hash. Adjust the TypeScript interface accordingly.

    frontend/src/__tests__/watcher.test.ts (NEW):
    Use Vitest with vi.useFakeTimers() for the 10s offline timer test. Mock EventSource by assigning a MockEventSource class to globalThis.EventSource before import. Mock fetchPage via vi.mock('../lib/api'). Use get() from svelte/store to read store values in assertions.
  </action>
  <verify>
    <automated>cd /home/mconceicao/work-others/foliom/frontend && npm run test -- --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    All 7 watcher.test.ts tests green. npm run test full suite still green (no regressions in existing 73+ tests).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire App.svelte, PageView.svelte conflict path, Sidebar.svelte status pill</name>
  <files>
    frontend/src/App.svelte,
    frontend/src/lib/pages/PageView.svelte,
    frontend/src/lib/components/Sidebar.svelte
  </files>
  <action>
    Read App.svelte, PageView.svelte, and Sidebar.svelte in full before editing.

    App.svelte: In onMount, call startWatcher() (import from '$lib/watcher'). Add window.addEventListener('beforeunload', stopWatcher) for cleanup. No other changes.

    PageView.svelte: Import externalConflict from '$lib/stores/watcher' and currentlyEditing from '$lib/stores/editing'. Add a $effect that watches $externalConflict:
    - If conflict is non-null AND $currentlyEditing !== null: set staleConflict = true (the existing Phase 3 rune), then externalConflict.set(null) to consume.
    - If conflict is non-null AND $currentlyEditing === null: call reload() (existing Phase 3 function), then externalConflict.set(null).
    The existing staleConflict banner markup and reload() function are ALREADY present from Phase 3 — do NOT duplicate or replace them. This $effect is the only addition.

    Sidebar.svelte: Import watcherStatus from '$lib/stores/watcher'. Add a status pill in the bottom-left area of the sidebar. Markup: a <div class="watcher-pill"> containing a single <span class="watcher-dot"> with a data-status attribute bound to $watcherStatus. CSS-only approach: three rules on data-status="connected" (green, static), data-status="reconnecting" (amber, @keyframes spin or pulse animation), data-status="offline" (grey, static). No JavaScript animation. Keep it subtle — small dot (~8px), no label text. Position with position: absolute; bottom: 0.75rem; left: 0.75rem if sidebar has position: relative, or equivalent layout-safe approach.
  </action>
  <verify>
    <automated>cd /home/mconceicao/work-others/foliom/frontend && npm run test 2>&1 | tail -20</automated>
  </verify>
  <done>
    Full frontend test suite passes (all existing + new tests). npm run build completes without TypeScript errors. The Sidebar renders a green dot when served against the real backend. Navigating between pages does not print "EventSource already closed — recreating" logs or create duplicate connections.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SSE stream → frontend | pages_updated data field is JSON from backend; must be parsed defensively |
| externalConflict store → PageView | Store mutation triggers staleConflict banner; must consume (set null) to avoid perpetual banner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | pages_updated JSON parse | mitigate | Wrap JSON.parse in try/catch in watcher.ts; malformed data logs a warning and is discarded without affecting stores |
| T-04-06 | Denial of Service | duplicate EventSource on navigation | mitigate | Module-level singleton guard: if es !== null and readyState !== CLOSED, startWatcher() returns immediately; test covers this case |
| T-04-07 | Information Disclosure | watcherStatus pill | accept | Pill only shows connectivity state (connected/reconnecting/offline); no page content or hashes exposed in UI |
| T-04-SC | Tampering | npm install (no new packages) | accept | No new npm packages added in this plan; existing Svelte/Vitest ecosystem; no slopcheck needed |
</threat_model>

<verification>
1. `npm run test` from frontend/ — all tests green including 7 new watcher tests.
2. `npm run build` — TypeScript clean, no errors.
3. Integration smoke with backend running: open browser, observe green dot in Sidebar, write a .md file from terminal, observe the page auto-reloads within ~1s if no block is being edited.
4. Conflict path: open a page, click a block to enter edit mode, write the same .md file from terminal — observe the existing "File changed externally" banner appears above the CM6 editor without dismissing the edit.
</verification>

<success_criteria>
- 7 new watcher.test.ts tests green. Full test suite (80+ tests) green. No regressions.
- TypeScript build clean (npm run build).
- Single EventSource per tab — no duplicates on navigation.
- pages_updated for non-editing page → silent reload.
- pages_updated while editing → staleConflict banner (Phase 3 component reused).
- index_reset → fetchPage() for current page.
- watcherStatus pill in Sidebar reflects connection state with CSS-only animation.
</success_criteria>

<output>
Create `.planning/phases/04-disk-sync/04-02-SUMMARY.md` when done.
</output>
