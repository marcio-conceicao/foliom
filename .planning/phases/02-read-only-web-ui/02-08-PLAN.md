---
phase: 02-read-only-web-ui
plan: 08
type: execute
wave: 6
depends_on: [02-07]
files_modified:
  - crates/cli/Cargo.toml
  - crates/core/Cargo.toml
  - Cargo.toml
  - crates/cli/src/bin/bench-gen.rs
  - crates/cli/src/bin/bench-rss.rs
  - crates/core/benches/cold_start.rs
  - crates/core/benches/README.md
  - scripts/bench_assert.py
  - .github/workflows/ci.yml
  - .planning/phases/02-read-only-web-ui/PERF-BASELINE.md
autonomous: true
requirements: [ACPT-02, ACPT-03, IDX-04]
tags: [phase-2, perf, ci, criterion, sysinfo, acceptance-gates]
must_haves:
  truths:
    - "`foliom-bench-gen --out /tmp/synth-5k --count 5000` produces 5000 synthetic .md files (70% journals, 30% pages) with realistic structure (TAB indents, code fences, drawers, properties, links/tags) reproducibly seeded."
    - "`cargo bench --bench cold_start` measures `Db::open + reindex(Full)` over /tmp/synth-5k and writes Criterion artifacts."
    - "`scripts/bench_assert.py` parses Criterion's estimates.json and exits non-zero if mean > 3 seconds (target 2s × 1.5 = 3s per D-35 and 02-RESEARCH §Performance Harness)."
    - "`foliom-bench-rss /tmp/synth-5k` spawns `foliom serve`, waits for steady state, measures RSS via sysinfo 0.30, prints `RSS: N MB (target: 300 MB, CI ceiling: 450 MB)`, exits non-zero if RSS > 450 MB."
    - "CI workflow runs frontend build → cargo build → cargo nextest → release build → Phase 1 inventory smoke → Phase 2 E2E smoke on Linux/macOS/Windows."
    - "CI bundle-size gate fails if `frontend/dist` exceeds 600 KB uncompressed."
    - "A dedicated CI job runs the perf gates (Linux-only) AFTER the test matrix passes."
    - "ACPT-02 CI assertion: cold start < 3s (3 × 10^9 ns). ACPT-03 CI assertion: RSS < 450 MB."
  artifacts:
    - path: crates/cli/src/bin/bench-gen.rs
      provides: "Synthetic 5k-corpus generator"
    - path: crates/cli/src/bin/bench-rss.rs
      provides: "RSS probe via sysinfo"
    - path: crates/core/benches/cold_start.rs
      provides: "Criterion cold-start benchmark"
    - path: scripts/bench_assert.py
      provides: "Parses Criterion estimates.json and asserts ceiling"
    - path: .github/workflows/ci.yml
      provides: "Updated CI matrix with Node + Rust + Phase 2 smoke + bench job"
    - path: .planning/phases/02-read-only-web-ui/PERF-BASELINE.md
      provides: "Recorded baseline metrics from the first green CI run"
  key_links:
    - from: ".github/workflows/ci.yml `bench` job"
      to: "scripts/bench_assert.py"
      via: "python3 scripts/bench_assert.py target/criterion/.../estimates.json 3_000_000_000"
      pattern: "bench_assert"
    - from: "bench-rss"
      to: "./target/release/foliom"
      via: "std::process::Command spawn + sysinfo read by pid"
      pattern: "Pid::from_u32"
---

<objective>
Close Phase 2 by landing the performance harness (ACPT-02 cold-start <2s ×1.5 ceiling, ACPT-03 RSS <300MB ×1.5 ceiling) and the CI matrix that exercises the entire Phase 2 vertical on Linux/macOS/Windows. Implement the synthetic 5k-corpus generator, Criterion benchmark, sysinfo-based RSS probe, and Python-based assertion script that gates merges.

Purpose: Makes the performance promise enforceable. Phase 2's stated reason for existing (cold start <2s, RAM <300MB on 5k notes) is now CI-verified.
Output: Green CI on all three OSes; perf gates pass (or surface a calibrated baseline that the team can iterate on).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-read-only-web-ui/02-CONTEXT.md
@.planning/phases/02-read-only-web-ui/02-RESEARCH.md
@.planning/phases/02-read-only-web-ui/02-07-SUMMARY.md

@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Synthetic 5k-corpus generator + Criterion cold-start benchmark + bench_assert.py</name>
  <files>crates/cli/Cargo.toml, crates/core/Cargo.toml, Cargo.toml, crates/cli/src/bin/bench-gen.rs, crates/core/benches/cold_start.rs, crates/core/benches/README.md, scripts/bench_assert.py</files>
  <behavior>
    - `cargo run --release --bin foliom-bench-gen -- --out /tmp/synth-5k --count 5000 --seed 42` produces exactly 5000 .md files: `journals/YYYY_MM_DD.md` × 3500 + `pages/Topic N.md` × 1500.
    - Files contain a mix of: TAB-indented bullets (depth 0–5, skewed to 0–2), occasional code fences with language tags, ~10% with `:LOGBOOK:` drawers, ~5% with `id::`/`collapsed::` properties, on average ~5 link/tag refs per block.
    - The generator is deterministic given the same `--seed`.
    - `cargo bench --bench cold_start` produces Criterion artifacts under `target/criterion/cold_start_5k/...`.
    - `scripts/bench_assert.py /path/to/estimates.json 3_000_000_000` reads `mean.point_estimate` and exits 0 if below the threshold, 1 otherwise. Prints the comparison.
    - On a dev laptop (M1-class), the bench completes one full sample in <3 seconds. CI runners (GHA Linux) may be 1.5–2× slower; the ×1.5 ceiling absorbs this per D-35 — if it doesn't (A8), this plan logs the actual numbers in `PERF-BASELINE.md` and the next plan or revision tunes the ceiling.
  </behavior>
  <action>
    Add dev-deps to `crates/core/Cargo.toml`: `criterion = "0.5"`, `tempfile = "3"` (under `[dev-dependencies]`). Add to `crates/cli/Cargo.toml`: `rand = "0.8"`, `rand_chacha = "0.3"`, `clap = { workspace = true, features = ["derive"] }` (already present). All via workspace inheritance where possible.

    Implement `crates/cli/src/bin/bench-gen.rs` per 02-RESEARCH §Performance Harness — Synthetic 5k corpus generator. Provide deterministic page-name pool, render helpers for journals (date-stamped) and pages (topical). Include realistic patterns:
    - bullets with `\t` indent, mix of depths 0/1/2/3/5
    - code fences (` ```rust ` / ` ```python ` / ` ```sql `) inside bullets at ~5% of bullets
    - `[[Topic N]]` and `[[YYYY_MM_DD]]` links scattered in text
    - `#tag` and `#[[multi word tag]]` chips
    - `id:: <uuid>` and `collapsed:: true` properties at ~5% of blocks
    - `:LOGBOOK: ... :END:` drawers on ~10% of journal pages
    - file sizes log-normal, median ~2 KB, occasional 30–50 KB files

    Wire as a separate binary target via `[[bin]] name = "foliom-bench-gen" path = "src/bin/bench-gen.rs"` in `crates/cli/Cargo.toml`.

    Implement `crates/core/benches/cold_start.rs` per 02-RESEARCH §Performance Harness — Cold-start benchmark. Add `[[bench]] name = "cold_start" harness = false` to `crates/core/Cargo.toml`. Use `tempfile::tempdir()` per iteration to force a cold start (fresh DB file). Read `/tmp/synth-5k` env override via `FOLIOM_BENCH_CORPUS` if set, default path `/tmp/synth-5k`. SKIP gracefully if the corpus doesn't exist (`eprintln!("skip — generate corpus first") ; return ;`).

    Use `Db::open_at(&db_path)` — confirm this constructor exists in `crates/core/src/storage/mod.rs` (it should: Plan 01-04 introduced location override; if it's named differently, e.g. `Db::open_with_path`, adjust accordingly).

    Implement `scripts/bench_assert.py`:
    ```python
    #!/usr/bin/env python3
    import json, sys
    path, ceiling_ns = sys.argv[1], int(sys.argv[2])
    with open(path) as f: data = json.load(f)
    mean_ns = data['mean']['point_estimate']
    print(f"mean={mean_ns/1e9:.3f}s ceiling={ceiling_ns/1e9:.3f}s")
    sys.exit(0 if mean_ns < ceiling_ns else 1)
    ```
    Mark executable. Use `python3` shebang (CI uses Python 3 by default on all GHA runners).

    Write `crates/core/benches/README.md` documenting: how to generate the corpus, how to run the bench locally, how to interpret Criterion's output, and the CI ceiling rationale (D-35).
  </action>
  <verify>
    <automated>cargo run --release --bin foliom-bench-gen -- --out /tmp/synth-5k --count 5000 --seed 42 &amp;&amp; cargo bench --bench cold_start &amp;&amp; python3 scripts/bench_assert.py target/criterion/cold_start_5k/*/new/estimates.json 3000000000</automated>
  </verify>
  <done>Corpus generates deterministically; bench runs and produces Criterion artifacts; bench_assert.py exits 0 on the dev machine (or surfaces actual numbers for tuning).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RSS probe binary (sysinfo) + CI workflow update + bundle-size gate + PERF-BASELINE.md</name>
  <files>crates/cli/Cargo.toml, Cargo.toml, crates/cli/src/bin/bench-rss.rs, .github/workflows/ci.yml, .planning/phases/02-read-only-web-ui/PERF-BASELINE.md</files>
  <behavior>
    - `./target/release/foliom-bench-rss /tmp/synth-5k` spawns `./target/release/foliom serve <corpus> --port 7350`, sleeps 5s, GETs `/api/pages` to warm caches, sleeps 2s, reads RSS via sysinfo by pid, kills the child, prints `RSS: N MB`, exits 0 if N <= 450, else 1.
    - CI workflow per 02-RESEARCH §CI Matrix Updates with these stages:
      - matrix: ubuntu-latest, macos-latest, windows-latest
      - Setup Node 20 (cache npm)
      - cd frontend && npm ci && npm run build
      - frontend bundle-size gate (fail if dist > 600 KB uncompressed)
      - frontend vitest
      - dtolnay/rust-toolchain@stable + Swatinem/rust-cache@v2
      - install cargo-nextest
      - cargo build --workspace --locked
      - cargo nextest run --workspace --no-fail-fast
      - cargo build --release --bin foliom --locked
      - Phase 1 inventory smoke (existing — keep unchanged)
      - Phase 2 E2E smoke (NEW: `foliom serve` background → curl /api/health + /api/pages → kill)
    - A separate `bench` job (Linux only, depends on `test`) generates corpus + runs cold-start bench + RSS probe + asserts both ceilings.
    - On Windows, the E2E smoke uses PowerShell (`Invoke-RestMethod`) per Pitfall 5; on Unix, uses curl + python3 assertion (matches Phase 1 pattern).
  </behavior>
  <action>
    Add `sysinfo = "0.30"` and `reqwest = { version = "0.12", default-features = false, features = ["blocking", "rustls-tls"] }` (or skip reqwest and hand-roll a 5-line HTTP GET against `std::net::TcpStream` to avoid a TLS dep) as `[dependencies]` of `crates/cli` AND as workspace deps. Prefer the hand-rolled path — reqwest is heavyweight for a single localhost GET.

    Implement `bench-rss.rs` per 02-RESEARCH §RSS measurement. Pin `sysinfo = "0.30"` exactly (A4 — units changed across versions). Add `[[bin]] name = "foliom-bench-rss" path = "src/bin/bench-rss.rs"` to `crates/cli/Cargo.toml`. The child binary is `./target/release/foliom serve ...`; resolve via `std::env::current_exe().parent().join("foliom")` for portability, falling back to `target/release/foliom`.

    Update `.github/workflows/ci.yml` per the YAML in 02-RESEARCH §CI Matrix Updates. Keep Phase 1's inventory smoke step verbatim (re-tested per OS as before). Add the new E2E smoke step (UNIX bash + Windows powershell variants) immediately after the release-binary-build step. Add a `bench` job (Linux only, needs: test) that does corpus gen + bench + RSS + bench_assert.py asserts.

    Bundle-size gate: simple `du -sk frontend/dist | cut -f1` comparison against 600. On Windows, GHA bash is git-bash which has `du`. The gate runs on every OS — fail-fast on the first OS to exceed (this should be deterministic; bundle size doesn't depend on the OS).

    Write `.planning/phases/02-read-only-web-ui/PERF-BASELINE.md` recording, after the first green CI run:
    - Cold start mean on Linux runner (ms)
    - RSS on Linux runner (MB)
    - Frontend bundle size (KB)
    - Date measured
    Leave a "Future tuning" section for noting drift over time.

    Confirm A8 (CI variance fits in ×1.5): if the first green run shows cold start near 3s, log a warning in PERF-BASELINE and recommend either tuning the index or raising the ceiling to ×2 in a follow-up plan/revision.
  </action>
  <verify>
    <automated>cargo build --release --workspace --locked &amp;&amp; ./target/release/foliom-bench-rss /tmp/synth-5k</automated>
  </verify>
  <done>RSS probe exits 0 on the dev machine; CI workflow file is valid YAML (lint via `yamllint` if available or by visual inspection of the diff) — actual CI green confirmation comes from pushing the branch and observing the matrix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow YAML → GitHub Actions runners | Standard CI trust model — workflow file is committed to the repo |
| Synthetic corpus generator → `/tmp` filesystem | Tests write to `/tmp/synth-5k`; never to the user's notes folder |
| sysinfo read of child pid → OS process API | Privileged read against own child; no privilege escalation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-25 | DoS | bench job consuming CI minutes | accept | Bench job needs `test` and runs only on push to main / PR. Disable on non-main branches if minutes become a problem (future tuning). |
| T-02-26 | Tampering | bench_assert.py reading attacker-controlled JSON | accept | estimates.json is generated by Criterion in CI; not external input. |
| T-02-SC | Tampering | new crates (sysinfo, rand, rand_chacha, criterion, tempfile, optional reqwest) | mitigate | All [ASSUMED] in 02-RESEARCH audit. sysinfo 0.30 pinned exactly for A4. rand + rand_chacha + criterion + tempfile are first-tier crates.io packages. |
</threat_model>

<verification>
- `cargo run --release --bin foliom-bench-gen -- --out /tmp/synth-5k --count 5000` produces 5000 .md files.
- `cargo bench --bench cold_start` writes Criterion artifacts.
- `python3 scripts/bench_assert.py target/criterion/.../new/estimates.json 3000000000` exits 0.
- `./target/release/foliom-bench-rss /tmp/synth-5k` reports a number and exits 0.
- CI passes on a test branch push (gitignored from this verification — operator confirms after merge of this plan).
</verification>

<success_criteria>
- ACPT-02 (cold start < 3s on 5k corpus) is CI-gated.
- ACPT-03 (RSS < 450 MB on 5k corpus) is CI-gated.
- ACPT-04 cross-platform CI remains green (Linux/macOS/Windows test job).
- IDX-04 partial coverage (`foliom serve --full` triggers a full reindex on startup per the `--full` flag from plan 02-01) — call this out in the SUMMARY as the seam where IDX-04 surfaced via Phase 2.
- Bundle size budget (< 600 KB uncompressed) is CI-gated.
- PERF-BASELINE.md records the first measured baseline.
</success_criteria>

<output>
Create `.planning/phases/02-read-only-web-ui/02-08-SUMMARY.md` documenting:
- First-run baseline numbers (cold start, RSS, bundle size).
- Whether A8 held (×1.5 absorbs CI variance) or had to be tuned.
- Any flakiness observed in the matrix (Windows long-path issues, macOS notarization warnings, etc.) and the fix.
- Final state of all Phase 2 requirements — coverage table mapping requirement → plan that delivered it → test that gates it.
</output>
